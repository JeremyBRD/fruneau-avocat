<%= simple_form_for @contact do |f| %>
  <%= f.input :name, label: 'Nom :', placeholder: 'Votre nom' %>
  <%= f.input :email, label: 'E-mail :', placeholder: 'Votre e-mail' %>
  <%= f.input :body, label: 'Message :', placeholder: 'Votre message' %>
  <%= f.submit 'ENVOYER' %>
<% end %>
